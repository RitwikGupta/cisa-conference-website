---
interface Props {
  title: string;
  intro?: string;
}
const { title, intro } = Astro.props;
---

<section class="page-intro wrap">
  <h1>{title}</h1>
  {intro && <p class="intro-text">{intro}</p>}
</section>
